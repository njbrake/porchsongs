# porchsongs Configuration
# Copy this file to .env and fill in your values

# CORS origins (comma-separated, * for all)
CORS_ORIGINS=*

# LLM Provider API Keys (set the ones you want to use)
# The server reads these from the environment — keys are never sent from the browser.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
# GROQ_API_KEY=
# GEMINI_API_KEY=

# Optional: restrict access to the API with a shared secret
# When set, all /api/ requests require an Authorization: Bearer <secret> header.
# APP_SECRET=
